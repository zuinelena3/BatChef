The `inst/extdata` directory contains an `.rda` file with a list comprising four elements:

* `coords`: the first two principal components used to visualize the data in a two-dimensional space derived from real-world datasets;
* `color`:the colors associated with the batch effects correction methods;
* `pca`: the results of the principal component analysis;
* `svm`: the results of the support vector machine model.

